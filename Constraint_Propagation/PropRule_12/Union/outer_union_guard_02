% Saved by Prover9-Mace4 Version 0.5, December 2007.

set(ignore_option_dependencies). % GUI handles dependencies

if(Prover9). % Options for Prover9
  assign(max_seconds, 60).
end_if.

if(Mace4).   % Options for Mace4
  assign(max_seconds, 60).
end_if.

formulas(assumptions).

%Moving onto binary operators, we here focus on the propagation of constraints under union

%Merging Guards should be pretty straigthforward
%This time, we want to see some condition over the unique attribute Z.

%Weird, it seems that the condition all x all y (S2(x,y) -> (z !=/= cond)) is pointless, and yet without the proof fails.
%Changing the mapping doesn't affect the result. Hmm, that's quite peculiar.

%CONSTRAINT A
%Guards

%PRESET 1 Z Stuff WORKS
%all x all y all z (S1(x,y,z) -> (z = cond)).
%all x all y (S2(x,y) -> (z = cond)).

%PRESET 2 Z Stuff WORKS
all x all y all z (S1(x,y,z) -> (z = cond)).
all x all y (S2(x,y) -> (z != cond)).

%PRESET 3 Z Stuff FAIL
%all x all y all z (S1(x,y,z) -> (z = cond)).

%MAPPINGS A TO B
%all x all y all z (R(x,y,z) <-> (S1(x,y,z) | S2(x,y))).

all x all y all z (R(x,y,z) <-> S1(x,y,z) | (S2(x,y) & Sz(z) & -(z = cond))).

end_of_list.

formulas(goals).

%Main Goal
all x all y all z (R(x,y,z) -> (z = cond)).

end_of_list.

