% Saved by Prover9-Mace4 Version 0.5, December 2007.

set(ignore_option_dependencies). % GUI handles dependencies

if(Prover9). % Options for Prover9
  assign(max_seconds, 60).
end_if.

if(Mace4).   % Options for Mace4
  assign(max_seconds, 60).
end_if.

formulas(assumptions).

%We present a series of formula showing the effects of constraint propagation for each pair of FOL constraint/RA operator
%Starting with the propagation of a multivalue dependency Y ->> Z after a set difference

%The same do not apply for MVD as for FDs.
%To see an example of a working MVD propagation using set difference, we suggest looking at the selection_mvd files.

%CONSTRAINT A
%Multivalue Dependency Y->>Z
all x all y all z all t((exists x1 exists t1 R1(x1,y,z,t1) & exists z1 R1(x,y,z1,t)) -> R1(x,y,z,t)).
all x all y all z all t((exists x1 exists t1 R2(x1,y,z,t1) & exists z1 R2(x,y,z1,t)) -> R2(x,y,z,t)).

%Soft Contain
exists x exists y exists z exists t (R2(x,y,z,t) -> R1(x,y,z,t)).

%MAPPINGS A TO B
all x all y all z all t (R(x,y,z,t)<-> R1(x,y,z,t) & -R2(x,y,z,t)).

end_of_list.

formulas(goals).

all x all y all z all t((exists x1 exists t1 R(x1,y,z,t1) & exists z1 R(x,y,z1,t)) -> R(x,y,z,t)).

end_of_list.

