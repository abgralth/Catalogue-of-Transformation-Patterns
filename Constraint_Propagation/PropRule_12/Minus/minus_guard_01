% Saved by Prover9-Mace4 Version 0.5, December 2007.

set(ignore_option_dependencies). % GUI handles dependencies

if(Prover9). % Options for Prover9
  assign(max_seconds, 60).
end_if.

if(Mace4).   % Options for Mace4
  assign(max_seconds, 60).
end_if.

formulas(assumptions).

%We present a series of formula showing the effects of constraint propagation for each pair of FOL constraint/RA operator
%Starting with the propagation of a guard y = cond after a set difference

%Pretty straightforward, work also if R2 is devoid of conditions

%CONSTRAINT A
%Guard
all x all y all z (R1(x,y,z) -> y = cond).

%all x all y all z (R2(x,y,z) -> y = cond).

%MAPPINGS A TO B
all x all y all z (R(x,y,z) <-> R1(x,y,z) & -R2(x,y,z)).

end_of_list.

formulas(goals).

all x all y all z (R(x,y,z) -> y = cond).

end_of_list.

