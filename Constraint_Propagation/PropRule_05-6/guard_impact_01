% Saved by Prover9-Mace4 Version 0.5, December 2007.

set(ignore_option_dependencies). % GUI handles dependencies

if(Prover9). % Options for Prover9
  assign(max_seconds, 60).
end_if.

if(Mace4).   % Options for Mace4
  assign(max_seconds, 60).
end_if.

formulas(assumptions).

%Playing a bit around the propagation rules related to guard dependencies, I propose the following documents
%The fifth propagation is unlikely to be modelable as its purpose is to show that it remains possible for a nullable condition 
%YZ = NULL to still hold in a table S1:(X,Z) devoid of Y.
%I'm not sure if this translates well to FOL, but we'll see

%The classic case is a relation R:(X,Y,Z) onto which is apply Y = NULL and YZ = NULL

%CONSTRAINT A
%Guards
exists x exists y exists z (R(x,y,z) -> y = $F).
exists x exists y exists z (R(x,y,z) -> (y = $F & z = $F)).

%MAPPINGS A TO B
all x all y all z (S1(x,z) <-> exists y (R(x,y,z) & y = $F)).
all x all y all z (S2(x,y,z) <-> R(x,y,z) & -(y = $F)).

end_of_list.

formulas(goals).

%all x all y all z (S2(x,y,z) -> y != $F).

exists x exists y exists z (S1(x,z) -> (y = $F & z = $F)).

end_of_list.

