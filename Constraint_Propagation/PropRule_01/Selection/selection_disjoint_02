% Saved by Prover9-Mace4 Version 0.5, December 2007.

set(ignore_option_dependencies). % GUI handles dependencies

if(Prover9). % Options for Prover9
  assign(max_seconds, 60).
end_if.

if(Mace4).   % Options for Mace4
  assign(max_seconds, 60).
end_if.

formulas(assumptions).

%We present a series of formula showing the effects of constraint propagation for each pair of FOL constraint/RA operator
%Starting with the propagation of a disjoint[R1,R2} after a selection

%Here, we want to see if the disjoint hold between S1 and R2, S2 and R2 and S1 and S2.
%It is the case for each pair.

%CONSTRAINT A
%Disjoint
all x all y (-R1(x,y) | -R2(x,y)).

%MAPPINGS A TO B
all x all y (S1(x,y) <-> R1(x,y) & x = cond).
all x all y (S2(x,y) <-> R1(x,y) & -(x = cond)).

end_of_list.

formulas(goals).

((all x all y (-S1(x,y) | -R2(x,y)))
&(all x all y (-S2(x,y) | -R2(x,y)))
&(all x all y (-S1(x,y) | -S2(x,y)))).

end_of_list.

