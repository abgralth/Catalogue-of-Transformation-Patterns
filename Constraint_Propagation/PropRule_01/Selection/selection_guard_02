% Saved by Prover9-Mace4 Version 0.5, December 2007.

set(ignore_option_dependencies). % GUI handles dependencies

if(Prover9). % Options for Prover9
  assign(max_seconds, 60).
end_if.

if(Mace4).   % Options for Mace4
  assign(max_seconds, 60).
end_if.

formulas(assumptions).

%We present a series of formula showing the effects of constraint propagation for each pair of FOL constraint/RA operator
%Starting with the propagation of a guard over Y after a selection
%We thus have two guard, one over Y and another over X leading to the split

%This time we check for the split

%CONSTRAINT A
%Guard
all x all y all z (R(x,y,z) -> (x = cond & y = cond)).

%MAPPINGS A TO B
all x all y all z (S1(x,y,z) <-> R(x,y,z) & x = cond).
all x all y all z (S2(x,y,z) <-> R(x,y,z) & -(x = cond)).

end_of_list.

formulas(goals).

(all x all y all z (S1(x,y,z) -> (x = cond & y = cond)))
&(all x all y all z (S2(x,y,z) -> (x = cond & y = cond))).

end_of_list.

